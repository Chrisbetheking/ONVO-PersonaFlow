import { FlaskConical, Info } from 'lucide-react'
import { useApp } from '../app/AppContext'
import { StatusPill } from '../shared/ui'
export function ExperimentsPage(){const{workspace}=useApp();return <section className="experiments-page" data-testid="experiments"><div className="demo-notice strong"><Info size={16}/>尚未形成真实业务结论。以下是可复现的验证设计，结果需由真实样本和人工标注产生。</div><div className="experiment-table"><div className="experiment-head"><span>指标</span><span>人工流程</span><span>PersonaFlow 流程</span><span>验证方式</span><span>状态</span></div>{workspace.enterprise.experiments.map(item=><article key={item.id}><span><strong>{item.metric}</strong><small>{item.name}</small></span><span>{item.manual_process}</span><span>{item.personaflow_process}</span><span>{item.validation}<small>样本 {item.sample_size} · {item.period}</small></span><span><StatusPill tone="info">{item.status}</StatusPill><small>{item.demo_flag?'Demo 设计':'业务实验'}</small></span></article>)}</div><section className="experiment-method"><FlaskConical/><div><strong>验证原则</strong><p>固定案例计时、专家双盲标注、审核日志和顾问人工确认。不会把演示数据包装成转化提升结果。</p></div></section></section>}
